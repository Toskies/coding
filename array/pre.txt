数组是存放在连续内存空间上的相同类型数据的集合

1. 数组下标都是从0开始的
2. 数组内存空间的地址是连续的:  在删除或者增添元素的时候，就难免要移动其他元素的地址
3. 数组的元素是不能删的，只能覆盖

方法：
1. 二分法：O(logn)   暴力解决：O(n)
2. 双指针：O(n)      暴力解决：O(n^2)
3. 滑动窗口: O(n)    暴力解决：O(n^2)
4. 前缀和:       重复利用计算过的子数组之和，从而降低区间查询需要累加计算的次数
